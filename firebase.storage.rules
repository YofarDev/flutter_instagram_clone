rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ponytail: auth-gated open bucket; per-folder scoping when it matters
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
